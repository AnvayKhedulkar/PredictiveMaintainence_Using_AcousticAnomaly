If you want to run the models locally and have a swagger UI and process a small recording:
    Run this in the vs code terminal, uvicorn src.API:app --host 0.0.0.0 --port 8000 --reload
    Open this link on your browser: http://localhost:8000/docs
